export default defineAppConfig({
   ui: {
      primary: 'green',
      gray: 'cool',

      /**
       * To import the icon dynamically
       */
      icons: {
         dynamic: true,
      },
   },
});
